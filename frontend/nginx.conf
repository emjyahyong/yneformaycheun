server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Compression des assets textuels.
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # SPA : toute route inconnue retombe sur index.html (routage React Router).
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache long pour les assets versionnés (hash dans le nom de fichier).
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Résolution DNS différée à la requête (pas au démarrage) : évite un crash
    # nginx si le backend "app" n'est pas encore déployé/résolvable.
    resolver 127.0.0.11 valid=10s;
    set $backend "app:8080";

    # Reverse proxy vers l'API Spring Boot (même origine => pas de CORS).
    location /api/ {
        proxy_pass http://$backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Documentation OpenAPI / Swagger UI, exposées via le proxy.
    location /swagger-ui/ { proxy_pass http://$backend; }
    location /v3/api-docs { proxy_pass http://$backend; }

    # Health check applicatif.
    location /actuator/ { proxy_pass http://$backend; }
}
