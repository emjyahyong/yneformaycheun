# URL de base de l'API consommée par le frontend.
# En dev, laisser vide pour utiliser le proxy Vite (/api -> http://localhost:8080).
# En production, renseigner l'URL publique de l'API, ex. https://api.exemple.com
VITE_API_URL=
